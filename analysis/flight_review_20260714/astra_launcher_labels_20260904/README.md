# Astra launcher names

All47 mission buttons, their advanced-list labels and the selected-mission label start with **Astra**. Verified using actual Tk widgets and the launch callback; the three current GPU commands remain unchanged. Script filenames, saved selection keys and experimental warnings are preserved. The idle launcher was closed gracefully and reopened with the new names.

[Machine-readable verification](receipt.json). No image-processing or detection behavior changed.
